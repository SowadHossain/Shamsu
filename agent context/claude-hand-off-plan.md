# SHAMSU v2.2 — Architecture Upgrade Roadmap

> Mode for this pass: **PLAN ONLY.** No source code is changed. The deliverable
> is this roadmap. Implementation happens in follow-up PRs once you approve the plan.
> Branch: work continues on **`dev_shamsu_V2.2`** (already the current branch — git on
> Windows treats `v2.2`/`V2.2` as the same ref, so no new branch is needed; only
> `agent context/SHAMSU_Architecture_v2.2.md` is currently uncommitted).

---

## Context — why this change

SHAMSU today is a working **v0.2.0** local coding agent: PRD → Django project, plus
Q&A/edit/bugfix/audit/test/doc workflows, a safety sandbox, sessions, and a 5-model
Ollama map. 379 tests pass; `ruff` is clean.

Your v2.2 research (`agent context/SHAMSU_Architecture_v2.2.md`) argues the system was
over-fit to one archetype (Django web apps) and one model layout, and that on 8GB VRAM
the way to Claude-Code-*like outcomes* is **not** bigger/more models but: a small model
doing a **small job** (typed "holes" in a pre-tested template), **iteration + a
deterministic judge** instead of model panels, **fewer model swaps**, and a **hardware
cookbook** that refuses models that don't fit. It also wants SHAMSU generalized beyond
games/Django into a **PRD → any-archetype** builder, borrowing harness patterns from
Odysseus (MCP tool layer, hardware-aware serving, verification-as-judge) while avoiding
its failed AI-council-voting experiment.

Intended outcome of the full program: a PRD produces a runnable project of the *right*
archetype, generated by an 8B/7B model filling tested holes, self-checked by real
build/test gates, on two anchor models with minimal swapping — "correct over fast."

### Ground truth vs. what the doc assumes (important)

The doc reads as if the LLM already fills holes during generation. It does **not**. The
current generate path is **100% deterministic template rendering**:
`shamsu/templates/django/writer.py` builds a `contents` dict from
`render_fixed_django_files` / `render_backend_django_files` / `render_frontend_django_files`
/ `render_django_test_files` — **no LLM calls**. LLM specialists (`coder`, `bugfix`,
etc.) are only used in the *interactive* workflows and the *error-feedback loop*.

**You chose "Full LLM hole-filling."** So the core new work is real: introduce
per-hole LLM generation (generate → self-review → deterministic gate → bounded retry)
and move web-crud generation onto it, keeping fixed boilerplate as template.

Already-present building blocks we reuse (do **not** rebuild):

- Swap control: `shamsu/runtime/ollama.py` → `unload_model`, `list_loaded_models`,
  `unload_shamsu_models`, session ownership, `keep_alive`.
- Per-call context assembly: `shamsu/context/builder.py` (`ContextBuilder.pack`),
  budgeting `shamsu/context/budget.py`, task-last prompt in `shamsu/llm/manager.py`.
- Schema-constrained JSON decoding: `LLMManager._generate(..., json_schema=...)` (Ollama
  native `format`), with `json_repair` fallback — reuse for the archetype enum classifier.
- Bounded fix loop with stall detection + escalation: `shamsu/agents/error_feedback_loop.py`.
- Deterministic gates: `shamsu/templates/django/checker.py`,
  `shamsu/templates/django/frontend_checker.py`, `shamsu/tools/django.py`
  (`DjangoSetupRunner`, `DjangoTestRunner`), `ast.parse` usage.
- Council (sequential draft→critique→reconcile): `shamsu/llm/council.py` — kept for the
  riskiest holes only; **not** used as a voting judge.
- Patch application + rollback: `shamsu/patch/engine.py`.

---

## Target architecture (end state)

1. **Two-anchor model map** — `qwen3:8b` (all thinking/text roles) + `qwen2.5-coder:7b`
   (all code roles), with a `SINGLE_MODEL_MODE` flag that routes everything to `qwen3:8b`
   for zero-swap measurement.
2. **Archetype registry** — the planner classifies the PRD into an archetype enum; the
   archetype selects a golden template + a typed generation manifest. Django becomes the
   `web-crud` / `rest-api` provider, not the center.
3. **Golden templates + typed holes** — 80–90% of shipped code is pre-tested template;
   the model fills small, independently-testable typed holes (one function/component each).
4. **Per-hole loop** — generate (coder) → self-review ≤2 (coder) → deterministic check
   → bounded retry on failure → next hole. Holes isolated so a fix can't cascade.
5. **Verification-as-judge (best-of-N)** — on a stalled hole, one alternate attempt
   (retry, or the other anchor), keep whichever passes build/tests. Never model voting.
6. **Swap-minimizing orchestration** — batch by model: all thinking, swap once, all
   coding + fix loop (coder stays warm), swap back only for a final review pass. Log swap count.
7. **Hardware cookbook** — an enforced allowlist that pins the model set to what fits 8GB
   and refuses to pull anything larger; surfaced via `doctor`.
8. **MCP tool layer (later)** — reimplement Odysseus's extensible-tool pattern (AGPL:
   reimplement, don't copy) so new tools plug in without new per-tool code.

---

## Phased roadmap

Each phase is independently shippable and keeps the suite green. Files named are the ones
that actually exist today (verified). "Reuse" points at code that must not be reinvented.

### Phase 0 — Branch + baseline

- Commit `agent context/SHAMSU_Architecture_v2.2.md` and this roadmap onto `dev_shamsu_V2.2`.
- Record the baseline: `pytest -q` (expect 379 passing) + `ruff check shamsu tests scripts`.
- **Exit:** baseline captured; branch confirmed.

### Phase 1 — Two-anchor model map + hardware cookbook  *(config-level, low risk)*

- `shamsu/runtime/models.py`: collapse `MODEL_SPECS` to `qwen3:8b` (roles: router, qa,
  planner, classifier, review, docs, summarizer) + `qwen2.5-coder:7b-instruct` (roles:
  coder, frontend, backend, tests, bugfix). Add `SINGLE_MODEL_MODE` (env/config) that
  maps every role to `qwen3:8b`. Add a `MODEL_COOKBOOK` allowlist + `is_allowed(model)`.
- `shamsu/runtime/ollama.py`: gate `ensure_model_available` / `pull_model*` on the
  cookbook — refuse (fail loud) to pull a model not on the allowlist.
- `shamsu/runtime/doctor.py`: add a `check_cookbook` DoctorCheck (installed models all fit).
- `shamsu/llm/manager.py`: `OLLAMA_MODELS`/`run_specialist` resolve role→model through a
  single helper that honors `SINGLE_MODEL_MODE`.
- Tests: update fixtures referencing `deepseek-coder`/`mistral`/`gemma3`.
- **Exit:** default = 2 models; single-model flag works; pulling an oversized model is refused.

### Phase 2 — Shared contract: archetype + manifest in `types.py`  *(team-contract change)*

- `shamsu/types.py`: add `class Archetype(str, Enum)` = `web_crud, rest_api, saas_fullstack, realtime_3d_game, generic_web`. Extend `ProjectSpec` with
  `archetype: Archetype = web_crud`, `archetype_confidence: float = 1.0`, and optional
  `archetype_spec: Any`. Add `Hole` (id, hole_type, target_file, signature/schema, desc,
  depends_on, specialist) and `GenerationManifest` (ordered `list[Hole]`).
- Per the doc's own rule, treat this as an explicit contract change (it's frozen-by-convention).
- **Exit:** types import cleanly; existing `ProjectSpec` callers still construct (defaults).

### Phase 3 — Archetype classifier

- New `shamsu/prd/classifier.py`: cheap rule-based keyword pass → archetype + confidence;
  low confidence escalates to a `qwen3:8b` **enum-constrained** classification (reuse
  `LLMManager._generate(json_schema=<enum>)`), low confidence overall → `generic_web`.
- `shamsu/prd/project.py`: `build_project_spec` sets `archetype` + `archetype_confidence`.
- **Exit:** the 3 fixture PRDs classify as `web_crud` with high confidence; a deliberately
  vague PRD falls back to `generic_web`.

### Phase 4 — Archetype registry + manifest abstraction

- New `shamsu/templates/registry.py`: `archetype → TemplateProvider` where a provider
  exposes `smoke_test()`, `build_manifest(project) -> GenerationManifest`, `render_fixed()`,
  and `check(project)`.
- Register today's `shamsu/templates/django/*` as the provider for **both** `web_crud`
  and `rest_api` (no aggressive file renames — avoid churn; wrap, don't move).
- `generic_web` provider = minimal static+API scaffold fallback.
- **Exit:** registry returns the Django provider for `web_crud`; `smoke_test` passes on the
  bare skeleton.

### Phase 5 — Swap-minimizing orchestration + archetype dispatch

- `shamsu/agents/full_pipeline.py`: generalize `FullDjangoPipeline` → dispatch on
  `project.archetype` via the registry. Enforce **batch-by-model** ordering: (1) classify
  + plan + build manifest on `qwen3:8b`; (2) swap once; (3) fill all holes + run fix loop
    on the coder (kept warm — never interleave a planner call); (4) swap back for the final
    review pass (review on by default). Use `runtime/ollama` primitives + `keep_alive` to
    control residency; **log a swap-count metric** per build.
- **Exit:** a full build touches at most two models; swap count is logged.

### Phase 6 — LLM hole-filling generator  *(the core new capability)*

- New `shamsu/generation/hole_filler.py` implementing the per-hole cycle from doc §5.5:
  generate (coder) → self-review ≤2 (hard cap) → **deterministic check** (`ast.parse`,
  Django checkers, `manage.py check`) → on fail, feed error to the warm coder, patch,
  re-check (bounded retries) → next hole. Holes isolated; same-error-twice escalates
  (re-plan/shrink/flag) instead of re-prompting identically.
- Reuse `ContextBuilder.pack` for a tiny per-hole context (skeleton slice + one hole),
  `LLMManager.run_specialist`, `PatchEngine`, and the Django checkers as the gate.
- Constrain output to **full-file replacement or strict unified diff** — never freeform.
- `shamsu/templates/django/writer.py`: for manifest holes whose `specialist` is an LLM
  role, call the hole-filler; keep fixed-template holes deterministic (still ~80–90% template).
- **Exit:** web-crud generates via hole-filling; self-review capped at 2; deterministic
  check is the sole pass/fail gate.

### Phase 7 — Verification-as-judge (best-of-N)

- Extend `shamsu/agents/error_feedback_loop.py`: on a stalled hole, allow **one** alternate
  attempt (retry same model, or swap to the other anchor once), keep whichever passes the
  deterministic check. Never a model vote. Use `shamsu/llm/council.py` (sequential
  draft→critique→reconcile) only for the riskiest holes (auth/billing/state), not as judge.
- **Exit:** a hole that fails once can be rescued by the alternate attempt, decided only by build/tests.

### Phase 8 — Context budget for 8–16K reality

- `shamsu/context/budget.py`: lower `TOTAL_BUDGET_DEFAULT`; add a tighter per-hole budget.
- `shamsu/llm/manager.py`: keep `num_ctx=8192`, add a 4096 fallback path when swap pressure
  is detected.
- **Exit:** per-hole prompts stay within the lowered ceiling; budget tests updated.

### Phase 9 — web-crud end-to-end proof + the one empirical measurement

- Run `tests/fixtures/prds/{todo,expense_tracker,blog}.md` through the new registry path.
- Record swap count, peak RAM (`tracemalloc`), first-run and post-fix test pass rate in
  `BENCHMARK.md`.
- Settle the doc's open question: **qwen3-only (single-model) vs. two-anchor** — build
  web-crud both ways and compare quality vs. swap cost.
- **Exit:** all 3 PRDs produce a runnable Django project; measurement documented.

### Phase 10+ — Roadmap only (not this program's first cut)

- `rest-api` provider, then `saas-fullstack` (auth/multi-tenant/billing stubs), then the
  `realtime-3d-game` template with **relay/room netcode** (transport/sync is template, not
  model work — doc §5).
- **MCP tool layer**: reimplement Odysseus's extensible-tool pattern (AGPL — reimplement,
  don't copy) in `shamsu/tools/` so new tools register without new per-tool code.

---

## Verification (per phase, end-to-end — not just unit tests)

- After every phase: `.\.venv\Scripts\python.exe -m pytest tests\ -q` and
  `.\.venv\Scripts\python.exe -m ruff check shamsu tests scripts` stay green.
- Phase 1: `python -m shamsu.runtime.ollama status` shows 2 required models; attempt to
  pull an off-cookbook model → refused; `/doctor` reports the cookbook check.
- Phase 3: run the classifier on the 3 fixtures + one vague PRD; assert archetypes/confidence.
- Phase 5: run a full build; grep session log for the swap-count event; assert ≤2 models touched.
- Phase 6/7: force a hole to fail (inject a bad field name), confirm the loop self-reviews,
  the deterministic gate catches it, and the alternate attempt rescues it.
- Phase 9: `python manage.py check` (0 errors), `migrate` (OK), `test` (≥80% after fix loop),
  `runserver` boots, `/admin` + `/login` render — driven headless via the existing Playwright
  browser tooling (`shamsu/tools/browser.py`).

---

## Risks & settled decisions

- **Contract churn**: `types.py` changes ripple; Phase 2 uses defaults so existing callers
  keep compiling. Land it early and alone.
- **Regression risk of moving off deterministic generation**: mitigated by keeping fixed
  boilerplate as template and gating every hole on a real check; the 379-test suite is the tripwire.
- **No Tier C / no bigger model** on 8GB: iteration + template coverage replace it (doc §8).
- **No AI-council voting** as a judge — accuracy comes from deterministic verification (doc §9).
- **Relay netcode is the game default**; authoritative netcode would need a hand-written
  template, not prompts (deferred to Phase 10+).

## First concrete action after approval

Because you chose **plan only**: commit `SHAMSU_Architecture_v2.2.md` + this roadmap to
`dev_shamsu_V2.2`, then stop. Implementation (Phase 1 onward) begins in a follow-up on your g

# SHAMSU — Codex Hand-Off Plan (Round 2): approval prompt is skipped in interactive use

> **Round 1 is DONE** (web answers, PRD milestone build, tokenizer, council in
> CodeEditWorkflow, input/toolbar fixes) — see `agent context/codex-handoff.md`
> and `PROGRESS.md`. This document replaces that plan with the next bug to fix.
> Repo root: `F:\Work\PROJECTS\shamsu\Shamsu`. Keep the suite green
> (`.\.venv\Scripts\python.exe -m pytest tests/ -q`, currently 379) and
> `ruff check shamsu tests scripts` clean.

## Context — the bug (interactive-only, high severity)

Reproduction (real terminal, NOT piped):

```
shamsu> please build me the game based on the prd file in the folder
<PRD Build Plan panel prints correctly>
<Approval Required panel prints>
Do you want to proceed?
  1. Yes
  2. No
>
PRD build not approved. Nothing was built.      <-- printed WITHOUT the user typing anything
shamsu> 1                                        <-- the user's "1" becomes the NEXT prompt
intent=qa ... "I cannot access files..."         <-- routed to QA, hallucinates
```

**Symptom:** the approval prompt does not wait for input. `input()` returns
immediately, `ask_approval_menu` reads it as an empty answer → "No" → the action
is auto-denied. The user's real answer ("1") is then consumed as the next REPL
prompt and routed to Q&A.

**Root cause:** the interactive approval prompt runs **while a Rich `Live`
spinner is active**. The main loop wraps every natural-language request in
`with console.status(_thinking_status_for_input(user_input)) as thinking:`
(`shamsu/cli/repl.py:2867`). The approval prompt (`ask_approval_menu` →
`input("> ")` in `shamsu/safety/approval.py`) is called deep inside that
`console.status` context. Rich's `Live`/`status` display and Python's built-in
`input()` cannot share a real TTY — the spinner's terminal control makes
`input()` return an empty string instead of blocking. (Streaming answers work
because `_stream_answer` explicitly calls `thinking_status.stop()` on the first
token; approval prompts never stop the spinner, so they break.)

**Why earlier smoke tests missed it:** every prior manual test piped stdin
(`"...\n1" | shamsu`). Piped (non-TTY) `input()` reads the pipe and is not
affected by the Live. The bug only appears on an interactive terminal.

**Second, path-specific aggravator:** `_handle_prd_build_request`
(`repl.py:1866`) builds its approval on a throwaway console:
`ApprovalManager(ask_approval, session_logger).ask(...)` → `ask_approval` with
`console=None` → a fresh `Console()`. That fresh console cannot even see the main
loop's live spinner, so it could never stop it. Every other approval site already
uses `_make_approval_manager(workspace, session_logger, console)` with the shared
main console.

This bug class affects **all** interactive approvals reached under the spinner:
PRD build, web search + read-results, agent-loop `write_file`/`run_command`,
patch approvals, `/plan-prd`, `/generate-prd`, and the `ask_remember_choice` /
`ask_clarifying_question` prompts.

## Fix (recommended: minimal + robust)

Goal: **no Rich `Live` may be active while any interactive `input()` runs**, and
**every approval prompt must use the one shared REPL console** so it can stop that
Live.

1. **One shared console for output AND prompts.**

   - `_handle_prd_build_request` (`repl.py:1866`): replace
     `ApprovalManager(ask_approval, session_logger)` with
     `_make_approval_manager(workspace, session_logger, console)` (needs the
     `workspace` + `console` already in scope). This routes the prompt through the
     numbered menu on the **main** console (and gains permission memory for free).
   - Audit the other direct `ApprovalManager(ask_approval, ...)` / `ask_approval`
     call sites that run under a `console.status` (`/plan-prd`, `/generate-prd`,
     the Django writer path) and make them use the shared console the same way.
2. **Stop the active spinner before reading input.** Add a tiny guarded helper
   (e.g. in `shamsu/safety/approval.py`):

   ```python
   def _pause_console_live(console) -> None:
       live = getattr(console, "_live", None)
       if live is not None:
           try:
               live.stop()
           except Exception:
               pass
   ```

   Call it immediately before the `input(...)` in `ask_approval_menu`,
   `ask_remember_choice` (both in `approval.py`), and `ask_clarifying_question`
   (`shamsu/safety/clarify.py`). Stopping the live is safe: the outer
   `with console.status(...)` calls `Live.stop()` again on exit and it is
   idempotent. After a prompt the spinner simply stays off for the rest of that
   turn (correct — we are now interacting/printing, not "thinking").

   > `console._live` is a private Rich attribute but has been stable for years;
   > the `getattr` + `try/except` keeps it safe. If preferred, Codex may instead
   > adopt Rich's global console (`rich.get_console()`) everywhere and gate on its
   > live — but the helper above is the smaller change.
   >
3. **Confirm the mechanism first.** Before/after the change, verify the hypothesis
   by temporarily commenting out the `with console.status(...)` wrapper at
   `repl.py:2867` and checking the approval now blocks correctly on a real
   terminal. This proves the Live is the cause and that the fix targets it.

### Optional larger cleanup (only if time allows)

Replace the single broad `console.status` wrapper around `_handle_request`
(`repl.py:2867`) with short-lived spinners scoped to the **non-interactive**
model calls only (routing in `_route_prompt`; the model call inside
`_run_code_edit`/`_run_bug_fix`/`_run_audit`/`_run_test_generation`/`_run_docs`),
and none around prompt-bearing/streaming handlers. This removes the whole class of
Live-vs-input conflicts structurally and lets the `thinking_status` threading
added for streaming be simplified. Higher risk; not required if steps 1–2 verify.

## Verification (MANDATORY — must be interactive, not piped)

Unit tests cannot reproduce the TTY `input()` behaviour, so a **real terminal**
check is required:

1. Open a fresh terminal, `cd` to a scratch folder containing a PRD, run `shamsu`.
2. `build the product from this prd` → at "Do you want to proceed?" the prompt
   **blocks**; typing `1`/`y`/Enter approves and the milestone build starts;
   typing `2`/`n` denies and the next prompt is NOT eaten.
3. `whats the weather in Dhaka today?` → both the search and the "read top
   results" approvals block and accept `1`.
4. During an agent build, a `write_file`/`run_command` approval blocks and accepts
   input.
5. Confirm `.\.venv\Scripts\python.exe -m pytest tests/ -q` still green (379+) and
   ruff clean.

Add a lightweight guard test where feasible (monkeypatch `builtins.input` to
return "1", call `ask_approval_menu` inside a `console.status(...)` on a
`force_terminal=True` console, assert it returns approved AND that
`getattr(console, "_live", None)` is stopped afterwards). Note in the test that it
guards the "spinner is paused before input" wiring, not the raw TTY behaviour.

## Update after implementing

- Append a short entry to `agent context/PROGRESS.md` ("Round 2 fix: interactive
  approval prompt skipped under the thinking spinner — fixed").
- Update `agent context/codex-handoff.md` (or add a Round-2 section) with the files
  changed and the interactive verification result.
