"""Path sandbox, command policy, and secret redaction.

Note the limit, as in v1: this is a policy layer, not an OS sandbox. Workspace
isolation (resource limits, restricted filesystem and network) is a deployment
concern described in plan section 24.1.

See plan section 24.
"""
